Code that trips rules with no fixtures of their own — the vendored
`@ashstack/effects/` rules — so `check:duplicates` can see them. Fixtures under
`packages/lint/fixtures/` cover everything written in this repo.
